# Direct dependencies, pinned exactly.
#
# mkdocs 1.6 is required for draft_docs and exclude_docs. 1.5 ignores both
# silently, which means the release vault would quietly publish everything.
mkdocs==1.6.1

# 9.7.0 released the Insiders features to everyone, which is what makes
# navigation.path (breadcrumbs) work at all.
# 9.7.7 fixed a DOM-based XSS in search suggestions. Do not go below it.
#
# Material for MkDocs reaches end of life on 5 November 2026. The site will
# keep building and serving after that, but there will be no more security
# fixes. Revisit over the winter break, not mid-semester.
mkdocs-material==9.7.7

